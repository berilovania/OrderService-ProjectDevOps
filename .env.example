# PostgreSQL
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USER=orders
DATABASE_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD
DATABASE_NAME=orders

# App
DATA_RETENTION_HOURS=24

# Metrics endpoint protection (optional — if set, /metrics requires Bearer token)
# METRICS_TOKEN=CHANGE_ME_GENERATE_RANDOM_TOKEN

# Grafana (local docker-compose only)
# GF_ADMIN_PASSWORD=CHANGE_ME_GRAFANA_PASSWORD
